import { ShowCompleteTasksPipe } from './show-complete-tasks.pipe';

describe('ShowCompleteTasksPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowCompleteTasksPipe();
    expect(pipe).toBeTruthy();
  });
});
